[profile.default]
src = "contracts"
out = "out"
libs = ["lib"]
solc_version = "0.8.27"
optimizer = true
optimizer_runs = 200
via_ir = true

[rpc_endpoints]
mumbai = "${MUMBAI_RPC_URL}"

[etherscan]
mumbai = { key = "${ETHERSCAN_API_KEY}", url = "https://api-mumbai.polygonscan.com/api" }

